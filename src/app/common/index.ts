/**
 * Common - Export centralizado
 * 
 * Importar desde: @common/*
 * 
 * Ejemplos:
 *   import { EstadoRendicion, TipoCalculo } from '@common/enums';
 *   import { ESTADO_LABEL, MAX_LENGTH } from '@common/constants';
 *   import { formatCurrency, round2 } from '@common/utils';
 *   import { PaginatedResponse, SelectOption } from '@common/types';
 */

export * from './enums';
export * from './constants';
export * from './utils';
export * from './types';
